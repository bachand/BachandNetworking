import XCTest

#if !canImport(ObjectiveC)
public func allTests() -> [XCTestCaseEntry] {
    return [
        // Modern Swift uses automatic test discovery, no need to list tests manually
    ]
}
#endif
